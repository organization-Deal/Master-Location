const installedRaw = `
บ้านหม้อ
ป.กุ้งเผา หลักสี่
ร้าน Replayground (ขอนแก่น)
Lism (เชียงใหม่)
โรงเหล้าแสงจันทร์ พระราม 2
ร้าน REST (รังสิต)
ร้าน THE REELS (นนทบุรี)
แบล็ค อินุ อิซากายะ นครอินทร์ พระราม 5
ป.กุ้งเผา ราชพฤกษ์ (เดี่ยว)
เฮ้าส์ คลอง 6 (HOUSE KLONG 6)
โรงเหล้าแสงจันทร์ ราชพฤกษ์
ร้านจันทร์เจ้าขา
ท่าช้างเชียงใหม่
Flick TU มธ.
สเตตัส แอร์พอร์ต สุวรรณภูมิ
ร้าน เยิ้มเชียงราย
Young โสด เชียงราย
Wake Up มหาดไทย (ลาดพร้าว 122)
แสงจันทร์ สุวรรณภูมิ
Doogu Dak ดูกู แดก (จันทบุรี)
Chalui Bar (เชียงใหม่)
หลบมุม เชียงราย
บ้านชอนนครสวรรค์
The Story (อยุธยา)
ล้านเพื่อน อยุธยา
ดั๊กคาเฟ่ อุบล
Steel Bar อยุธยา
โฟล์คพระนคร อุดรธานี
Friendly Sriracha ศรีราชา
Claro นิมมาน
Balance (จันทบุรี)
ร้านยาวปัย-เหล้าปั่น (คลองสามวา)
12Bar โชว์ระบบ
Chiva Bar ชีวาบาร์ องค์รักษ์
SIPLUX PATONG ภูเก็ต
Host-Pital - บาร์โฮสนนทบุรี
Say Yes.BKK
Hang Out - แฮงค์เอาท์ โคราช
ท่าช้างบางแสน
ร้าน BF บ้านฝรั่ง (โคราช)
ร้านปีกไม้ สระบุรี (เดี่ยว)
90 Bar & Bistro LKB
Good Bar
บ้านเพื่อน บางแสน ชลบุรี
FENIX SALAYA
โคตรบาร์ เชียงราย
ก้องสยาม สมุย
ตลาดหัวมุม
โรงเหล้าไม้หมอน พิษณุโลก
โฟล์คพระนคร โคราช
อุ่นไอดิน
Blue Bear Bubble
RAD BAR เชียงราย
บางบาร์ โคราช
THEJUK (เดอะจั๊ค) Day&Night
The Great Wall Bistro & Nightlife
เมก้า พลาซ่า สะพานเหล็ก
ร้านมุมอร่อย
ร้านบ้านกูเอง pub&bistro โคราช
Bar and Restaurant (ศรีนครรินทร์)
เจ้าสัว Cafe & Restaurant เชียงราย
The Wolverine Udonthani
Morning Tonight
Max Muay Thai Stadium Pattaya
ร้านกงสี อุดรธานี
Feelgood bar ราชบุรี
Sugar เชียงใหม่
Take Up เชียงใหม่
T-Bar เชียงใหม่
sound up เชียงใหม่
Stories Salaya
โกวเล้ง ศรีราชา
กงสี 公司 ร้อยเอ็ด
The Fox Maejo เชียงใหม่
Buddy12ห้วยแก้วเชียงใหม่
Game Zone Bar อุดรธานี
ลาบหนองจันทร์ by winpub
Moto Bar แจ้งวัฒนะ
Young โสด เชียงราย
Makinnee ขอนแก่น (มากินนี่)
Station CMI
Beyond club & live Music
Boy Club เชียงราย
Analog อนาล็อก กำแพงเพชร
The airport - Nakhon Pathom
Tidlom เพชรบุรี
Walk in 88
เฌอปอนด์ ผับ แอนด์ เรสเตอรองท์
SLAY ร้อยเอ็ด
มานี Bar & Restaurant นครพนม
ร้านATLAS HOUSE lopburi
Maxzeed
ท่าช้างเชียงใหม่
U-Like Pakchong
รื่นรมย์ท่าบ่อ หนองคาย
โรงเหล้าบางปู สมุทรปราการ
อร่อยวังหิน78
ร้านลานปิติ พัทยา
แสงจันทร์ ศรีนครินทร์
สกาลา นิคมบางปู สมุทรปราการ
Sound Up
เคลิ้ม Bar&Bristro
Warp Pattaya
โรงเหล้าแสงตะวัน หาดใหญ่
Commuse เชียงใหม่
นั่งเพลิน ลาดกระบัง
Mr.k Party House PATTAYA
เติมสุข สกลนคร
Rere khaosan
Rock Zone Live Music Bar หัวหิน
เพลินบุรี แคมป์ปิ้ง ไร่ขิง
Newbie Huahin
เกาลูนบาร์ ขอนแก่น
โรงเบียร์สุขนิยม บ้านบึง
Mixx66 อุบล
Snail Korat
THE JUK ราชบุรี
ร้าน เยิ้มเชียงราย
WHY Mae Sot
สำราญ มหาชน ศรีสะเกษ
Chill Chill Bar
สุขนิยม น่าน
ทรงวาด K. Kyle Worasetthayanon
Ninety nine K. Kyle Worasetthayanon
Blabla จ.เลย K. Kyle Worasetthayanon
Pee2 Bar โคราช K. Kyle Worasetthayanon
The one club
Lotto bar บางซื่อ
แฮงค์บาร์ ชัยภูมิ
ทรงวาด
รื่นรมย์ท่าบ่อ หนองคาย
ช่างสำราญ ขุขันธ์ ศรีสะเกษ
ฅนคอนหวัน ตำนานเพลง(ตาก)
โฮงสี เชียงใหม่
Friendface & Restaurant
Crossroad Rock Bar เชียงใหม่
Level Up.BKK
THE CASSETTE แหลมฉบัง
บ.มดยิ้ม
ยักษ์ 79
โรงเหล้าแสงจันทร์ นวนคร
เวลาริญ
FIX CLUB THABO NONGKHAI
ตะวันแดงสาดแสงจันทร์ ณ สกลนคร
สุขสม
ตากอากาศ since 2013 กาฬสินธุ์
นิทาน Bar&Restaurant Nithan. ปทุมธานี
โรงเหล้าแสนคำ
บ้านม่า เพชรบุรี
Together Udonthani อุดรธานี
Fix Club อุดร
พวกกัน 10 สุโขทัย
ระหัดน้ำ
66Zaap Bar ตลาดเรือบิน
Taste D
Give me 5
`;

const installed = installedRaw.trim().split('\n').map((name,i)=>({id:`INST-${String(i+1).padStart(3,'0')}`,name:name.trim()}));
const STORE_KEY='deal_location_workspace_v1';
const stageMap={new:'ร้านใหม่',research:'กำลังหาข้อมูล',ready:'พร้อมติดต่อ',contacted:'ติดต่อแล้ว',interested:'ร้านสนใจ',survey:'นัดสำรวจพื้นที่',negotiating:'กำลังเจรจา',won:'ปิดร้านได้',lost:'ไม่สำเร็จ'};
const activeStages=['new','research','ready','contacted','interested','survey','negotiating'];

const defaultState={
  settings:{leadTarget:300,wonTarget:20,month:'2026-08'},
  leads:[
    {id:'LOC-0001',venue:'Example On Tour Venue',province:'เชียงใหม่',area:'เมือง',type:'ร้านเหล้า / ผับ',source:'วงดนตรีออกทัวร์',band:'ตัวอย่างวง A',sourceUrl:'',contact:'',phone:'',channel:'Instagram / TikTok',owner:'ทีมโลเคชั่น A',stage:'ready',score:70,nextFollowUp:'2026-09-01',capacity:350,hours:'19:00–02:00',dealModel:'',mapUrl:'',notes:'เห็นวงดังเล่นหลายวง / ควรหาเบอร์ผู้จัดการ',createdAt:'2026-08-31T10:10:00',updatedAt:'2026-08-31T10:10:00',qTraffic:true,qAudience:true,qLateHours:true,qSpace:false,qPower:false,qDeal:false,qReputation:true},
    {id:'LOC-0002',venue:'Example New Pub KK',province:'ขอนแก่น',area:'เมือง',type:'Bar & Bistro',source:'Facebook',band:'',sourceUrl:'',contact:'คุณต้น',phone:'',channel:'Facebook',owner:'ทีมโลเคชั่น B',stage:'contacted',score:55,nextFollowUp:'2026-09-02',capacity:220,hours:'18:00–01:00',dealModel:'',mapUrl:'',notes:'ทักเพจแล้ว รอผู้จัดการตอบ',createdAt:'2026-08-31T11:15:00',updatedAt:'2026-08-31T11:15:00',qTraffic:true,qAudience:true,qLateHours:true,qSpace:false,qPower:false,qDeal:false,qReputation:false}
  ],
  tours:[
    {id:'T-1',band:'ตัวอย่างวง A',venue:'Example On Tour Venue',province:'เชียงใหม่',date:'2026-09-05',url:'',status:'lead'},
    {id:'T-2',band:'ตัวอย่างวง B',venue:'New Venue Korat',province:'นครราชสีมา',date:'2026-09-07',url:'',status:'new'}
  ],
  activities:[
    {at:'2026-08-31T11:15:00',type:'contact',text:'ทีมโลเคชั่น B ติดต่อ Example New Pub KK'},
    {at:'2026-08-31T10:10:00',type:'lead',text:'เพิ่ม Example On Tour Venue จากวงดนตรีออกทัวร์'}
  ]
};

let state=loadState();let currentView='dashboard';let filter={q:'',stage:'all',owner:'all',dup:'all'};
function loadState(){try{const raw=localStorage.getItem(STORE_KEY);return raw?JSON.parse(raw):structuredClone(defaultState)}catch(e){return structuredClone(defaultState)}}
function save(){localStorage.setItem(STORE_KEY,JSON.stringify(state));syncHeaderCounts()}
function norm(s=''){return String(s).toLowerCase().replace(/\([^)]*\)/g,' ').replace(/[\s\-_.&/]+/g,' ').replace(/ร้าน|bar|bistro|pub|restaurant|club|cafe|โรงเหล้า/g,'').trim()}
function installedMatch(name){const n=norm(name);if(!n)return null;return installed.find(x=>{const a=norm(x.name);return a===n || (n.length>6 && (a.includes(n)||n.includes(a)))})||null}
function leadDuplicate(name,id=''){const n=norm(name);return state.leads.find(l=>l.id!==id && norm(l.venue)===n)||null}
function scoreLead(l){if(typeof l.score==='number')return l.score;return ['qTraffic','qAudience','qLateHours','qSpace','qPower','qDeal','qReputation'].reduce((s,k)=>s+(l[k]?{qTraffic:25,qAudience:20,qLateHours:10,qSpace:10,qPower:5,qDeal:15,qReputation:15}[k]:0),0)}
function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function sourceLabel(s=''){return s==='วงดนตรี On Tour'?'วงดนตรีออกทัวร์':s}
function fmtDate(s){if(!s)return'-';const d=new Date(s);if(isNaN(d))return s;return d.toLocaleDateString('th-TH',{day:'2-digit',month:'short'})}
function monthLeads(){const m=state.settings.month;return state.leads.filter(l=>(l.createdAt||'').slice(0,7)===m)}
function syncHeaderCounts(){document.getElementById('navLeadCount').textContent=state.leads.length;document.getElementById('navInstalledCount').textContent=installed.length;const ml=monthLeads();const won=ml.filter(l=>l.stage==='won').length;const lt=+state.settings.leadTarget||300,wt=+state.settings.wonTarget||20;document.getElementById('miniLeads').textContent=ml.length;document.getElementById('miniLeadTarget').textContent=lt;document.getElementById('miniWon').textContent=won;document.getElementById('miniWonTarget').textContent=wt;document.getElementById('miniLeadBar').style.width=Math.min(100,ml.length/lt*100)+'%';document.getElementById('miniWonBar').style.width=Math.min(100,won/wt*100)+'%'}
function toast(t){const el=document.getElementById('toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1800)}
function setPage(title,eyebrow='ฝ่ายพัฒนาโลเคชั่น'){document.getElementById('pageTitle').textContent=title;document.getElementById('pageEyebrow').textContent=eyebrow}

function render(){syncHeaderCounts();document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.view===currentView));const root=document.getElementById('viewRoot');if(currentView==='dashboard')renderDashboard(root);if(currentView==='leads')renderLeads(root);if(currentView==='pipeline')renderPipeline(root);if(currentView==='tour')renderTour(root);if(currentView==='installed')renderInstalled(root);if(currentView==='team')renderTeam(root);if(currentView==='settings')renderSettings(root)}

function renderDashboard(root){setPage('ภาพรวม');const ml=monthLeads(),won=ml.filter(l=>l.stage==='won'),contacted=ml.filter(l=>['contacted','interested','survey','negotiating','won'].includes(l.stage)),ready=ml.filter(l=>l.stage==='ready'),lt=+state.settings.leadTarget||300,wt=+state.settings.wonTarget||20;const stageCounts=Object.fromEntries(Object.keys(stageMap).map(s=>[s,state.leads.filter(l=>l.stage===s).length]));const max=Math.max(1,...Object.values(stageCounts));const overdue=state.leads.filter(l=>l.nextFollowUp && new Date(l.nextFollowUp+'T23:59:59')<new Date() && !['won','lost'].includes(l.stage));root.innerHTML=`
<div class="grid-kpi">
 <div class="kpi"><div class="kpi-top"><span class="label">รายชื่อร้านเดือนนี้</span><span class="badge">เป้าหมาย ${lt}</span></div><div class="value">${ml.length}</div><small>เหลืออีก <b class="target">${Math.max(0,lt-ml.length)}</b> ร้าน</small><div class="progress"><i style="width:${Math.min(100,ml.length/lt*100)}%"></i></div></div>
 <div class="kpi"><div class="kpi-top"><span class="label">ติดต่อแล้ว</span><span class="badge blue">ติดต่อแล้ว+</span></div><div class="value">${contacted.length}</div><small>อัตราการติดต่อ <b class="target">${ml.length?Math.round(contacted.length/ml.length*100):0}%</b></small><div class="progress"><i style="width:${ml.length?contacted.length/ml.length*100:0}%"></i></div></div>
 <div class="kpi"><div class="kpi-top"><span class="label">ร้านพร้อมให้ติดต่อ</span><span class="badge amber">พร้อมติดต่อ</span></div><div class="value">${ready.length}</div><small>ข้อมูลครบแล้ว พร้อมโทร/ทัก</small><div class="progress"><i style="width:${Math.min(100,ready.length/30*100)}%"></i></div></div>
 <div class="kpi"><div class="kpi-top"><span class="label">ปิดโลเคชั่นได้</span><span class="badge green">เป้าหมาย ${wt}</span></div><div class="value">${won.length}</div><small>เหลืออีก <b class="target">${Math.max(0,wt-won.length)}</b> ร้าน</small><div class="progress red"><i style="width:${Math.min(100,won.length/wt*100)}%"></i></div></div>
</div>
<div class="section-grid">
 <div class="panel"><div class="panel-head"><div><h3>ภาพรวมขั้นตอนการหาร้าน</h3><p>เห็นทันทีว่าทีมติดอยู่ที่ “หาไม่ได้” หรือ “ติดต่อไม่เป็น”</p></div><button class="mini-btn" data-go="pipeline">เปิดขั้นตอนการติดต่อ</button></div><div class="metric-list">${Object.entries(stageMap).map(([s,label])=>`<div class="metric-row"><span>${label}</span><div class="bar"><i style="width:${stageCounts[s]/max*100}%"></i></div><b>${stageCounts[s]}</b></div>`).join('')}</div></div>
 <div class="panel"><div class="panel-head"><div><h3>งานติดตามที่ต้องทำ</h3><p>${overdue.length} รายการเลยกำหนด</p></div></div>${overdue.length?`<div class="feed">${overdue.slice(0,6).map(l=>`<div class="feed-item"><div class="feed-icon">!</div><div><strong>${esc(l.venue)}</strong><span>${esc(l.owner||'ยังไม่มอบหมาย')} · ${stageMap[l.stage]}</span></div><time>${fmtDate(l.nextFollowUp)}</time></div>`).join('')}</div>`:`<div class="empty"><strong>ไม่มีงานติดตามค้าง</strong>ทุกงานอยู่ในกำหนด</div>`}</div>
</div>
<div class="section-grid">
 <div class="panel"><div class="panel-head"><div><h3>ระบบกันร้านเก่าถูกเอามาขายซ้ำ</h3><p>ทุกชื่อใหม่จะเทียบกับร้านที่ติดตั้งแล้ว ${installed.length} รายการอัตโนมัติ</p></div></div><div class="callout"><b>กติกา:</b> ถ้าชื่อร้านตรง/ใกล้เคียงร้านที่เคยติดตั้งแล้ว ระบบขึ้นว่า “ซ้ำ” ทันที และไม่ควรนับเป็น KPI ร้านใหม่ จนกว่าจะยืนยันว่าเป็น “สาขาใหม่” หรือ “เจ้าของใหม่”</div></div>
 <div class="panel"><div class="panel-head"><div><h3>กิจกรรมล่าสุด</h3><p>กิจกรรมล่าสุดของทีม</p></div></div><div class="feed">${state.activities.slice(0,6).map(a=>`<div class="feed-item"><div class="feed-icon">${a.type==='contact'?'☎':'+'}</div><div><strong>${esc(a.text)}</strong><span>${fmtDate(a.at)}</span></div></div>`).join('')||'<div class="empty">ยังไม่มีกิจกรรม</div>'}</div></div>
</div>`;bindGo();}

function filteredLeads(){return state.leads.filter(l=>{const q=filter.q.toLowerCase();const text=[l.venue,l.province,l.band,l.owner,l.contact].join(' ').toLowerCase();const dup=!!installedMatch(l.venue);return(!q||text.includes(q))&&(filter.stage==='all'||l.stage===filter.stage)&&(filter.owner==='all'||l.owner===filter.owner)&&(filter.dup==='all'||(filter.dup==='yes'&&dup)||(filter.dup==='no'&&!dup))})}
function renderLeads(root){setPage('ร้านเป้าหมาย','ระบบค้นหาและติดตามร้าน');const owners=[...new Set(state.leads.map(l=>l.owner).filter(Boolean))];const rows=filteredLeads();root.innerHTML=`<div class="toolbar"><div class="toolbar-group"><input class="search" id="leadSearch" value="${esc(filter.q)}" placeholder="ค้นหาร้าน / จังหวัด / วง / ผู้ดูแล"><select class="select" id="stageFilter"><option value="all">ทุกสถานะ</option>${Object.entries(stageMap).map(([k,v])=>`<option value="${k}" ${filter.stage===k?'selected':''}>${v}</option>`).join('')}</select><select class="select" id="ownerFilter"><option value="all">ทุกคน</option>${owners.map(o=>`<option ${filter.owner===o?'selected':''}>${esc(o)}</option>`).join('')}</select><select class="select" id="dupFilter"><option value="all">ทั้งหมด</option><option value="no" ${filter.dup==='no'?'selected':''}>เฉพาะร้านใหม่</option><option value="yes" ${filter.dup==='yes'?'selected':''}>ร้านซ้ำกับร้านเก่า</option></select></div><div class="toolbar-group"><button class="btn ghost" id="exportCsvBtn">ส่งออก CSV</button><button class="btn primary" id="inlineAddBtn">+ เพิ่มร้านเป้าหมาย</button></div></div>
<div class="table-wrap"><table class="data-table"><thead><tr><th>ร้าน / โลเคชั่น</th><th>แหล่งที่มา</th><th>สถานะ</th><th>คะแนน</th><th>ผู้ติดต่อ</th><th>ผู้รับผิดชอบ</th><th>ติดตามครั้งถัดไป</th><th>ตรวจร้านซ้ำ</th><th></th></tr></thead><tbody>${rows.map(l=>{const dup=installedMatch(l.venue);const sc=scoreLead(l);return`<tr class="${dup?'duplicate-row':''}"><td class="venue-cell"><strong>${esc(l.venue)}</strong><small>${esc(l.province||'-')} ${l.area?'· '+esc(l.area):''}</small></td><td>${esc(sourceLabel(l.source)||'-')}<br><small>${esc(l.band||'')}</small></td><td><span class="badge ${l.stage==='won'?'green':l.stage==='lost'?'red':l.stage==='interested'?'blue':''}">${stageMap[l.stage]||l.stage}</span></td><td><span class="score ${sc>=70?'high':sc>=45?'mid':'low'}">${sc}</span>/100</td><td>${esc(l.contact||'-')}<br><small>${esc(l.phone||l.channel||'')}</small></td><td>${esc(l.owner||'ยังไม่มอบหมาย')}</td><td>${fmtDate(l.nextFollowUp)}</td><td>${dup?`<span class="badge red">ซ้ำ: ${esc(dup.name)}</span>`:'<span class="badge green">ร้านใหม่</span>'}</td><td><div class="actions"><button class="mini-btn" data-edit="${l.id}">เปิด</button><button class="mini-btn" data-brief="${l.id}">ข้อมูลก่อนติดต่อ</button></div></td></tr>`}).join('')||`<tr><td colspan="9"><div class="empty"><strong>ไม่พบร้าน</strong>ลองเปลี่ยนตัวกรองหรือเพิ่มร้านใหม่</div></td></tr>`}</tbody></table></div>`;
 document.getElementById('leadSearch').oninput=e=>{filter.q=e.target.value;render()};document.getElementById('stageFilter').onchange=e=>{filter.stage=e.target.value;render()};document.getElementById('ownerFilter').onchange=e=>{filter.owner=e.target.value;render()};document.getElementById('dupFilter').onchange=e=>{filter.dup=e.target.value;render()};document.getElementById('inlineAddBtn').onclick=()=>openLeadModal();document.getElementById('exportCsvBtn').onclick=()=>exportLeads(rows);root.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openLeadModal(state.leads.find(l=>l.id===b.dataset.edit)));root.querySelectorAll('[data-brief]').forEach(b=>b.onclick=()=>copyBrief(state.leads.find(l=>l.id===b.dataset.brief)));}

function renderPipeline(root){setPage('ขั้นตอนการติดต่อ','ติดต่อ → ปิดโลเคชั่น');const cols=['new','ready','contacted','interested','survey','negotiating','won'];root.innerHTML=`<div class="kanban">${cols.map(s=>{const list=state.leads.filter(l=>l.stage===s);return`<div class="kanban-col"><div class="kanban-head"><strong>${stageMap[s]}</strong><b>${list.length}</b></div>${list.map(l=>`<div class="kanban-card" data-edit="${l.id}"><strong>${esc(l.venue)}</strong><small>${esc(l.province||'-')} · ${esc(l.owner||'ยังไม่มอบหมาย')}</small><div class="kanban-meta"><span class="badge">${scoreLead(l)}/100</span><small>${l.nextFollowUp?'↻ '+fmtDate(l.nextFollowUp):''}</small></div></div>`).join('')||'<div class="empty">ว่าง</div>'}</div>`}).join('')}</div>`;root.querySelectorAll('[data-edit]').forEach(c=>c.onclick=()=>openLeadModal(state.leads.find(l=>l.id===c.dataset.edit)));}

function renderTour(root){setPage('ข้อมูลทัวร์วงดนตรี','วงดนตรี → ร้าน → ร้านเป้าหมาย');const bands=[...new Set(state.tours.map(t=>t.band).filter(Boolean))];root.innerHTML=`<div class="tour-grid"><div class="panel"><div class="panel-head"><div><h3>วงดนตรี / แหล่งข้อมูล</h3><p>ใช้เป็น “แหล่งสร้างรายชื่อร้านใหม่” ไม่ใช่แค่เก็บโน้ต</p></div><button class="mini-btn" id="tourPasteBtn">+ วางตารางทัวร์</button></div><div class="band-list">${bands.map(b=>{const ts=state.tours.filter(t=>t.band===b);return`<div class="band-card"><strong>♫ ${esc(b)}</strong><small>${ts.length} ร้านที่พบ · ${new Set(ts.map(x=>x.province)).size} จังหวัด</small></div>`}).join('')||'<div class="empty">ยังไม่มีข้อมูลวง</div>'}</div></div><div class="panel"><div class="panel-head"><div><h3>ร้านที่พบจากตารางทัวร์</h3><p>ทุกที่ที่วงไปเล่น = ร้านเป้าหมาย จนกว่าจะตรวจว่าเคยติดตั้งแล้ว</p></div></div><div class="table-wrap"><table class="data-table" style="min-width:700px"><thead><tr><th>วันที่</th><th>วง</th><th>ร้าน</th><th>จังหวัด</th><th>ผลตรวจ</th><th></th></tr></thead><tbody>${state.tours.sort((a,b)=>String(a.date).localeCompare(String(b.date))).map(t=>{const dup=installedMatch(t.venue);const lead=state.leads.find(l=>norm(l.venue)===norm(t.venue));return`<tr class="${dup?'duplicate-row':''}"><td>${fmtDate(t.date)}</td><td>${esc(t.band)}</td><td><b>${esc(t.venue)}</b></td><td>${esc(t.province||'-')}</td><td>${dup?'<span class="badge red">ติดตั้งแล้ว</span>':lead?'<span class="badge blue">อยู่ในรายการติดตาม</span>':'<span class="badge green">ร้านใหม่</span>'}</td><td>${!dup&&!lead?`<button class="mini-btn" data-tourlead="${t.id}">เพิ่มเป็นร้านเป้าหมาย</button>`:''}</td></tr>`}).join('')}</tbody></table></div></div></div><div class="panel" style="margin-top:16px"><div class="callout"><b>ขั้นตอนการทำงานที่ควรใช้:</b> 1) เก็บตารางงานของหลายวง → 2) แตกเป็นรายชื่อร้าน → 3) ระบบตัดร้านที่เคยติดตั้ง → 4) หาข้อมูลร้านใหม่ให้ครบ ผู้ติดต่อ + ความจุ + หลักฐาน → 5) เปลี่ยนสถานะเป็นพร้อมติดต่อ → 6) ทีมโทร/ทักและติดตามจนปิดได้หรือไม่สำเร็จ</div></div>`;document.getElementById('tourPasteBtn').onclick=openResearchModal;root.querySelectorAll('[data-tourlead]').forEach(b=>b.onclick=()=>{const t=state.tours.find(x=>x.id===b.dataset.tourlead);openLeadModal({venue:t.venue,province:t.province,source:'วงดนตรีออกทัวร์',band:t.band,sourceUrl:t.url,stage:'new',owner:'ยังไม่มอบหมาย',nextFollowUp:t.date})});}

function renderInstalled(root){setPage('ร้านที่ติดตั้งแล้ว','ห้ามนำร้านเก่ากลับมาขายซ้ำ');const unique=new Map();installed.forEach(x=>{const k=norm(x.name);if(!unique.has(k))unique.set(k,[]);unique.get(k).push(x.name)});const dups=[...unique.values()].filter(a=>a.length>1);const q=(filter.installedQ||'').toLowerCase();const list=installed.filter(x=>x.name.toLowerCase().includes(q));root.innerHTML=`<div class="installed-summary"><span class="badge green">${installed.length} รายการทั้งหมด</span><span class="badge">${unique.size} ชื่อหลังรวมรูปแบบ</span><span class="badge amber">${dups.length} กลุ่มชื่อซ้ำในข้อมูลต้นทาง</span></div><div class="toolbar"><input class="search" id="installedSearch" value="${esc(filter.installedQ||'')}" placeholder="ค้นหาร้านที่เคยติดตั้งแล้ว"><button class="btn ghost" id="exportInstalledBtn">ส่งออกรายชื่อร้านที่ติดตั้งแล้ว</button></div><div class="table-wrap"><table class="data-table" style="min-width:650px"><thead><tr><th>#</th><th>ร้านที่ติดตั้งแล้ว</th><th>คีย์ตรวจชื่อซ้ำ</th><th>สถานะ</th></tr></thead><tbody>${list.map((x,i)=>{const n=norm(x.name),same=unique.get(n)||[];return`<tr><td>${i+1}</td><td><b>${esc(x.name)}</b></td><td><small>${esc(n)}</small></td><td>${same.length>1?`<span class="badge amber">ซ้ำในต้นฉบับ ${same.length} ครั้ง</span>`:'<span class="badge green">ติดตั้งแล้ว</span>'}</td></tr>`}).join('')}</tbody></table></div>`;document.getElementById('installedSearch').oninput=e=>{filter.installedQ=e.target.value;render()};document.getElementById('exportInstalledBtn').onclick=()=>downloadCSV('installed_locations.csv',['ชื่อโลเคชั่น'],installed.map(x=>[x.name]));}

function renderTeam(root){setPage('ผลงานทีม','วัดผลแบบมีตัวเลข');const owners=[...new Set(state.leads.map(l=>l.owner||'ยังไม่มอบหมาย'))];root.innerHTML=`<div class="owner-grid">${owners.map(o=>{const ls=state.leads.filter(l=>(l.owner||'ยังไม่มอบหมาย')===o),contacted=ls.filter(l=>['contacted','interested','survey','negotiating','won'].includes(l.stage)).length,won=ls.filter(l=>l.stage==='won').length,ready=ls.filter(l=>l.stage==='ready').length;return`<div class="owner-card"><h3>${esc(o)}</h3><div class="owner-metrics"><div><strong>${ls.length}</strong><span>ร้านทั้งหมด</span></div><div><strong>${contacted}</strong><span>ติดต่อแล้ว+</span></div><div><strong>${won}</strong><span>ปิดได้</span></div></div><div class="progress" style="margin-top:12px"><i style="width:${ls.length?contacted/ls.length*100:0}%"></i></div><small style="font-size:9px;color:#888">พร้อมติดต่อ: ${ready} · อัตราปิด: ${ls.length?Math.round(won/ls.length*100):0}%</small></div>`}).join('')}</div><div class="panel" style="margin-top:16px"><div class="panel-head"><div><h3>กติกาวัดผลงานทีมโลเคชั่น</h3><p>ไม่ใช้คำว่า “วันนี้หาไม่ได้” โดยไม่มีตัวเลขอธิบาย</p></div></div><div class="metric-list"><div class="metric-row"><span>ผลงานการหาร้าน</span><div>จำนวนร้านใหม่ที่ไม่ซ้ำ + มี URL แหล่งข้อมูล</div><b>รายวัน</b></div><div class="metric-row"><span>อัตราพร้อมติดต่อ</span><div>% ร้านที่หาผู้ติดต่อ + ข้อมูลขั้นต่ำครบจนพร้อมติดต่อ</div><b>≥60%</b></div><div class="metric-row"><span>จำนวนครั้งที่ติดต่อ</span><div>จำนวนการโทร/ทักจริง พร้อมวันที่ติดต่อล่าสุด + วันติดตามครั้งถัดไป</div><b>รายวัน</b></div><div class="metric-row"><span>อัตราปิดร้าน</span><div>ร้านที่ปิดได้ ÷ ร้านที่ติดต่อแล้ว</div><b>ติดตามผล</b></div></div></div>`;}

function renderSettings(root){setPage('ตั้งค่าและข้อมูล');root.innerHTML=`<div class="settings-grid"><div class="settings-card"><h3>KPI ประจำเดือน</h3><label>เดือนที่วัด KPI<input id="monthSet" type="month" value="${state.settings.month}"></label><label>เป้าหมายรายชื่อร้านใหม่<input id="leadTargetSet" type="number" min="1" value="${state.settings.leadTarget}"></label><label>เป้าหมายร้านที่ปิดได้<input id="wonTargetSet" type="number" min="1" value="${state.settings.wonTarget}"></label><button class="btn primary" id="saveSettings">บันทึกเป้าหมาย</button></div><div class="settings-card"><h3>ส่งออกเพื่อส่งต่อ Master Location</h3><p style="font-size:10px;color:#777">ส่งออกเฉพาะร้านที่สถานะ = ปิดร้านได้ เป็น CSV โดยจัดหัวคอลัมน์ให้ใกล้กับไฟล์ Master Location เดิม เพื่อนำไปทำสัญญา/เอกสารต่อ</p><button class="btn" id="handoffBtn">ส่งออกร้านที่ปิดได้ → Master CSV</button></div><div class="settings-card"><h3>สำรองข้อมูลระบบ</h3><button class="btn" id="backupBtn">ส่งออกไฟล์สำรอง JSON</button> <label style="margin-top:14px">นำเข้าข้อมูลสำรอง JSON<input id="restoreFile" type="file" accept="application/json"></label></div><div class="settings-card danger-zone"><h3>ล้างข้อมูลตัวอย่าง</h3><p style="font-size:10px;color:#777">ล้างรายชื่อร้าน / ข้อมูลทัวร์ / กิจกรรมในเบราว์เซอร์นี้ และกลับเป็นข้อมูลตัวอย่างเริ่มต้น</p><button class="btn danger" id="resetBtn">ล้างระบบ</button></div></div>`;document.getElementById('saveSettings').onclick=()=>{state.settings.month=document.getElementById('monthSet').value;state.settings.leadTarget=+document.getElementById('leadTargetSet').value||300;state.settings.wonTarget=+document.getElementById('wonTargetSet').value||20;save();render();toast('บันทึก KPI แล้ว')};document.getElementById('handoffBtn').onclick=exportHandoff;document.getElementById('backupBtn').onclick=()=>downloadBlob('location-workspace-backup.json',JSON.stringify(state,null,2),'application/json');document.getElementById('restoreFile').onchange=restoreBackup;document.getElementById('resetBtn').onclick=()=>{if(confirm('ล้างข้อมูลในเบราว์เซอร์นี้และกลับเป็นข้อมูลตัวอย่างใช่ไหม?')){state=structuredClone(defaultState);save();render();toast('ล้างข้อมูลแล้ว')}};}

function openLeadModal(data=null){const modal=document.getElementById('leadModal'),form=document.getElementById('leadForm');form.reset();document.getElementById('modalTitle').textContent=data?.id?'แก้ไขร้านเป้าหมาย':'เพิ่มร้านเป้าหมาย';if(data){Object.keys(data).forEach(k=>{const el=form.elements[k];if(!el)return;if(el.type==='checkbox')el.checked=!!data[k];else el.value=(k==='source'&&data[k]==='วงดนตรี On Tour')?'วงดนตรีออกทัวร์':(data[k]??'')})}else{form.elements.owner.value='ยังไม่มอบหมาย';form.elements.stage.value='new'}checkDuplicateLive();showModal(modal)}
function showModal(modal){document.getElementById('modalBackdrop').classList.remove('hidden');modal.classList.remove('hidden')}
function closeModals(){document.getElementById('modalBackdrop').classList.add('hidden');document.querySelectorAll('.modal').forEach(m=>m.classList.add('hidden'))}
function checkDuplicateLive(){const form=document.getElementById('leadForm'),w=document.getElementById('duplicateWarning');const v=form.elements.venue.value,id=form.elements.id.value;const inst=installedMatch(v),lead=leadDuplicate(v,id);if(inst||lead){w.classList.remove('hidden');w.innerHTML=`⚠ <b>พบชื่อซ้ำ/ใกล้เคียง:</b> ${inst?'เคยติดตั้งแล้ว “'+esc(inst.name)+'” ':''}${lead?'มีในรายการติดตามแล้ว “'+esc(lead.venue)+'”':''}<br>ถ้าเป็นคนละสาขา ให้ใส่ชื่อสาขา/จังหวัดให้ชัดก่อนบันทึก`}else w.classList.add('hidden')}
function formToLead(form){const o=Object.fromEntries(new FormData(form).entries());['qTraffic','qAudience','qLateHours','qSpace','qPower','qDeal','qReputation'].forEach(k=>o[k]=form.elements[k].checked);const weights={qTraffic:25,qAudience:20,qLateHours:10,qSpace:10,qPower:5,qDeal:15,qReputation:15};o.score=Object.keys(weights).reduce((s,k)=>s+(o[k]?weights[k]:0),0);o.capacity=+o.capacity||0;return o}

document.getElementById('leadForm').addEventListener('submit',e=>{e.preventDefault();const form=e.currentTarget,o=formToLead(form),now=new Date().toISOString();if(!o.venue.trim())return;const inst=installedMatch(o.venue);if(inst&&!confirm(`ร้านนี้ใกล้เคียง ร้านที่ติดตั้งแล้ว: ${inst.name}\nยืนยันว่าเป็นสาขาใหม่/มีเหตุผลต้องเพิ่มใช่ไหม?`))return;if(o.id){const i=state.leads.findIndex(l=>l.id===o.id);o.createdAt=state.leads[i].createdAt;o.updatedAt=now;state.leads[i]=o;state.activities.unshift({at:now,type:'edit',text:`อัปเดต ${o.venue} → ${stageMap[o.stage]}`})}else{o.id='LOC-'+String(Date.now()).slice(-7);o.createdAt=now;o.updatedAt=now;state.leads.unshift(o);state.activities.unshift({at:now,type:'lead',text:`เพิ่ม ${o.venue} จาก ${o.source}`})}save();closeModals();render();toast('บันทึกร้านแล้ว')});
document.getElementById('leadForm').elements.venue.addEventListener('input',checkDuplicateLive);

function openResearchModal(){document.getElementById('researchPaste').value='';document.getElementById('researchPreview').innerHTML='';showModal(document.getElementById('researchModal'))}
function parseResearch(text){return text.split('\n').map(x=>x.trim()).filter(Boolean).map((line,i)=>{const p=line.split('|').map(x=>x.trim());if(p.length>=2)return{id:`P-${i}`,band:p[0],venue:p[1],province:p[2]||'',date:p[3]||'',url:p[4]||''};return{id:`P-${i}`,band:'',venue:p[0],province:'',date:'',url:''}})}
document.getElementById('researchPaste').addEventListener('input',e=>{const items=parseResearch(e.target.value);document.getElementById('researchPreview').innerHTML=items.length?`<div class="preview-list">${items.slice(0,30).map(x=>{const d=installedMatch(x.venue);return`<div class="preview-row"><span>${esc(x.band?x.band+' → ':'')}${esc(x.venue)} ${x.province?'· '+esc(x.province):''}</span>${d?'<span class="badge red">ติดตั้งแล้ว</span>':'<span class="badge green">ร้านใหม่</span>'}</div>`}).join('')}</div><small>${items.length} บรรทัด · ${items.filter(x=>installedMatch(x.venue)).length} ซ้ำกับร้านที่ติดตั้งแล้ว</small>`:''});
document.getElementById('importResearchBtn').onclick=()=>{const items=parseResearch(document.getElementById('researchPaste').value);if(!items.length)return;let added=0,tours=0;items.forEach(x=>{state.tours.push({id:'T-'+Date.now()+'-'+tours++,band:x.band,venue:x.venue,province:x.province,date:x.date,url:x.url,status:'new'});if(!installedMatch(x.venue)&&!state.leads.some(l=>norm(l.venue)===norm(x.venue))){const now=new Date().toISOString();state.leads.unshift({id:'LOC-'+Date.now()+'-'+added,venue:x.venue,province:x.province,area:'',type:'ร้านเหล้า / ผับ',source:x.band?'วงดนตรีออกทัวร์':'ข้อมูลที่วางเข้าระบบ',band:x.band,sourceUrl:x.url,contact:'',phone:'',channel:'ยังไม่ทราบ',owner:'ยังไม่มอบหมาย',stage:'new',score:0,nextFollowUp:x.date,capacity:0,hours:'',dealModel:'',mapUrl:'',notes:'',createdAt:now,updatedAt:now});added++}});state.activities.unshift({at:new Date().toISOString(),type:'lead',text:`นำเข้าข้อมูล ${items.length} รายการ → ได้ร้านใหม่ ${added} ร้าน`});save();closeModals();currentView='tour';render();toast(`เพิ่มร้านเป้าหมายใหม่ ${added} ร้าน`)};

function copyBrief(l){const dup=installedMatch(l.venue);const text=`ข้อมูลเตรียมก่อนติดต่อร้าน\nร้าน: ${l.venue}\nจังหวัด/โซน: ${l.province||'-'} ${l.area||''}\nประเภท: ${l.type||'-'}\nแหล่งที่มา: ${sourceLabel(l.source)||'-'} ${l.band?'('+l.band+')':''}\nหลักฐาน: ${l.sourceUrl||'-'}\nความจุโดยประมาณ: ${l.capacity||'-'}\nเวลาเปิด: ${l.hours||'-'}\nคะแนน: ${scoreLead(l)}/100\nผู้ติดต่อ: ${l.contact||'-'} ${l.phone||''}\nช่องทาง: ${l.channel||'-'}\nซ้ำกับร้านที่ติดตั้งแล้ว: ${dup?'ใช่ - '+dup.name:'ไม่ซ้ำ'}\n\nสิ่งที่ต้องรู้ก่อนติดต่อ:\n${l.notes||'-'}\n\nเป้าหมายการคุย: ขอคุยผู้มีอำนาจตัดสินใจ → อธิบายโมเดลวางตู้ชกมวย → เช็กจุดวาง/ไฟ/จำนวนลูกค้า/เงื่อนไขแบ่งรายได้ → นัดสำรวจพื้นที่`;
 navigator.clipboard?.writeText(text).then(()=>toast('คัดลอกข้อมูลก่อนติดต่อแล้ว')).catch(()=>prompt('คัดลอกข้อมูลก่อนติดต่อ',text));}
function exportLeads(rows){const headers=['ID','ชื่อร้าน','จังหวัด','โซน','ประเภท','แหล่งที่มา','วง/คำค้นหา','URL แหล่งข้อมูล','สถานะ','คะแนน','ผู้ติดต่อ','เบอร์โทร','ช่องทาง','ผู้ดูแล','ติดตามครั้งถัดไป','ซ้ำกับร้านที่ติดตั้งแล้ว','หมายเหตุ'];downloadCSV('target_locations.csv',headers,rows.map(l=>[l.id,l.venue,l.province,l.area,l.type,sourceLabel(l.source),l.band,l.sourceUrl,stageMap[l.stage],scoreLead(l),l.contact,l.phone,l.channel,l.owner,l.nextFollowUp,installedMatch(l.venue)?.name||'',l.notes]))}
function exportHandoff(){const rows=state.leads.filter(l=>l.stage==='won');const headers=['ชื่อโลเคชั่น','วันที่หาโลเคชั่นได้','ผู้ดูแล','ที่อยู่ติดตั้งตู้','ลิงก์แผนที่โลเคชั่น','จังหวัด (ตามที่อยู่ร้าน)','ประเภทพื้นที่','ประเภทตู้ที่จะติดตั้ง','ความจุของร้าน (จำนวน)','อีเมล','เบอร์โทรศัพท์','รายละเอียดของร้าน','ช่องทางติดต่อ','เวลาเปิดปิด','รูปแบบค่าตอบแทน'];downloadCSV('won_locations_master_handoff.csv',headers,rows.map(l=>[l.venue,(l.createdAt||'').slice(0,10),l.owner,'',l.mapUrl,l.province,l.type,'ตู้ชกมวย',l.capacity,'',l.phone,l.notes,l.channel,l.hours,l.dealModel]));toast(`ส่งออก ${rows.length} ร้านที่ปิดได้`)}
function csvCell(v){const s=String(v??'');return/[",\n]/.test(s)?'"'+s.replace(/"/g,'""')+'"':s}
function downloadCSV(name,headers,rows){downloadBlob(name,'\ufeff'+[headers,...rows].map(r=>r.map(csvCell).join(',')).join('\n'),'text/csv;charset=utf-8')}
function downloadBlob(name,text,type){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
function restoreBackup(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{state=JSON.parse(r.result);save();render();toast('นำเข้าข้อมูลสำรองแล้ว')}catch(err){alert('ไฟล์ JSON ไม่ถูกต้อง')}};r.readAsText(f)}
function bindGo(){document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{currentView=b.dataset.go;render()})}

document.querySelectorAll('.nav-item').forEach(b=>b.onclick=()=>{currentView=b.dataset.view;render();document.getElementById('sidebar').classList.remove('open')});document.getElementById('quickAddBtn').onclick=()=>openLeadModal();document.getElementById('pasteResearchBtn').onclick=openResearchModal;document.getElementById('menuBtn').onclick=()=>document.getElementById('sidebar').classList.toggle('open');document.getElementById('modalBackdrop').onclick=closeModals;document.querySelectorAll('[data-close-modal]').forEach(b=>b.onclick=closeModals);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModals()});
render();
